#
# GCF-840 DROP TEMPORARY TABLE rewriting fails on tables with unicode names
#

--source include/galera_cluster.inc

--connection node_1
--character_set utf8
CREATE DATABASE `some database`;
CREATE DATABASE `база данни`;

CREATE TEMPORARY TABLE `some database`.`временна таблица` (f1 INTEGER) ENGINE=InnoDB;
CREATE TABLE `база данни`.`some table` (f1 INTEGER) ENGINE=InnoDB;
DROP TABLE `some database`.`временна таблица`, `база данни`.`some table`;

--connection node_2
SELECT COUNT(*) = 0 FROM INFORMATION_SCHEMA.TABLES WHERE TABLE_NAME = 'some table';

--connection node_1
DROP DATABASE `some database`;
DROP DATABASE `база данни`;
